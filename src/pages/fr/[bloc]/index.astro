---
import BlocPage from "../../../components/pages/BlocPage.astro";
import type { BlocId } from "../../../lib/entries";

export function getStaticPaths() {
  return (["governance", "prompts", "build", "evaluation"] as const).map(
    (bloc) => ({ params: { bloc } }),
  );
}
---

<BlocPage lang="fr" bloc={Astro.params.bloc as BlocId} />
